# -----------------------------------------------------------------------------
# DJANGO CORE SETTINGS
# -----------------------------------------------------------------------------
# A secret key for a particular Django installation.
# Generate a new one for production.
DJANGO_SECRET_KEY=your-super-secret-key-here

# Set to 'production' for production environment or 'local' for development
DJANGO_ENV=local

# -----------------------------------------------------------------------------
# DJANGO SECURITY SETTINGS
# -----------------------------------------------------------------------------
# A comma-separated list of strings representing the host/domain names that this Django site can serve.
# For production, this should be your backend domain/container name(s).
# Example: trivia-api.rajivwallace.com,trivia-backend

ALLOWED_HOSTS=localhost,127.0.0.1,trivia-backend

# A comma-separated list of origins that are trusted for cross-site requests.
# For production, this should be your frontend's domain(s).
# Example: https://trivia.rajivwallace.com,https://www.trivia.rajivwallace.com

CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173

# -----------------------------------------------------------------------------
# POSTGRESQL DATABASE SETTINGS
# -----------------------------------------------------------------------------
POSTGRES_DB=postgres-core
POSTGRES_USER=trivia-prod
POSTGRES_PASSWORD=your-secure-postgres-password
POSTGRES_HOST=trivia-prod
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# FRONTEND (VITE/REACT) SETTINGS
# -----------------------------------------------------------------------------
# The base URL for the backend API that the frontend will connect to.
# This should be the public URL of your Django backend in production.
# Example: https://trivia-api.rajivwallace.com

VITE_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# AI SERVICE (GEMINI) SETTINGS
# -----------------------------------------------------------------------------
# Your API key from Google AI Studio or LLM of choice
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Specify models for different environments
GEMINI_MODEL="gemini-model"